# 使用文档（CASTT 参数修改说明）

本文档说明本仓库中与 `confidence-aware-STT (CASTT)` 相关的关键参数在哪里修改，以及建议调参顺序。

## 1. 主开关与仿真入口

- 入口脚本：`code/main.m`
- 目前主流程保留并运行：`CKF`、`STT`、`CASTT`

如果你只想改变仿真规模/噪声模型，优先改：
- `num_sim`、`num_agent`、`num_neighbor`
- `Noise_model`
- `bearing_noise` / `bearing_rate_noise` / `position_noise`

---

## 2. CASTT 参数定义位置（最重要）

文件：`code/GetParams.m`

在每个 `Noise_model` 分支下，`filters_params.castt.conf` 定义了所有质量感知参数：

- `w = [w1,w2,w3,w4]`：质量分数权重（det/center/area/track）
  - 当前验证阶段默认 `w1=0`（不用 detector confidence）
  - 代码会对 `w2:w4` 自动归一化
- `eta`：质量分数时间平滑系数
- `q_min`：平滑后质量下限
- `gamma`：质量到方差映射指数
- `theta_max`：中心性角度归一化阈值
- `r_ref`：距离代理归一化参考距离
- `N_ref`：连续命中帧数参考值
- `tau`：连续丢失指数衰减系数
- `track_gate`：命中判定的创新角阈值（弧度）
- `e_cam`：相机光轴单位向量
- `sigma0`、`sigma_good`、`sigma_bad`：等效噪声方差映射相关参数

---

## 3. CASTT 算法逻辑位置

文件：`code/Filters/CASTTUpdate.m`

该文件中完成以下步骤：
1. 计算每个观测的 `s_center / s_area / s_track`
2. 计算 `q_raw`，再做时间平滑得到 `q_bar`
3. 由 `q_bar` 映射 `sigma2_jk`
4. 构造 `Rinv_jk = I / sigma2_jk`，用于测量加权
5. 执行与 STT 相同的 consensus + correction 更新

并在 `castt.agent(i)` 中维护：
- `q_bar`
- `n_hit`
- `n_lost`
- `sigma2`

---

## 4. 初始化位置

文件：`code/IninFilter.m`

CASTT 初始化项包括：
- `castt.agent(i).x`
- `castt.agent(i).M`
- `castt.agent(i).q_bar`
- `castt.agent(i).n_hit`
- `castt.agent(i).n_lost`
- `castt.agent(i).sigma2`

---

## 5. 更新调用位置

文件：`code/FilterUpdate.m`

每个时间步都会调用：
- `CKFUpdate(...)`
- `STTUpdate(...)`
- `CASTTUpdate(...)`

---

## 6. 结果查看位置

- 轨迹图：`code/PlotEstTraj.m`
- 误差曲线/箱线图：`code/AnalysisData.m`

CASTT 结果字段：
- `data_save(i).castt`
- `error_struct.castt.*`

---

## 7. 建议调参顺序

1. 固定 `w=[0,0.4,0.3,0.3]`
2. 先调 `eta`（0.2~0.4）控制稳定性
3. 再调 `track_gate`、`N_ref`、`tau`（影响 s_track）
4. 最后调 `sigma_bad/sigma_good` 与 `gamma`（影响降权强度）

如果你想让低质量观测更“狠”地降权：
- 增大 `sigma_bad`
- 或增大 `gamma`


---

## 8. A/B/C 消融实验开关（CASTT）

文件：`code/GetParams.m`

新增参数：
- `filters_params.castt.conf.ablation_mode = "C";`
  - `"A"`：只在 `e^{meas}` 里用自适应权重（`y` 自适应，`S` 固定）
  - `"B"`：只在 `S_{i,k}` 里用自适应权重（`S` 自适应，`y` 固定）
  - `"C"`：两边都用自适应权重（默认）
- `filters_params.castt.conf.fixed_Rinv_scale = 1/filters_params.castt.r1;`
  - 作为 A/B 模式中“固定权重分支”的常量

实现位置：`code/Filters/CASTTUpdate.m`，通过 `W_S` 和 `W_y` 两个通道分离控制。

建议做法：
1. 固定随机种子和其他参数不变
2. 分别设置 `ablation_mode = "A" / "B" / "C"` 跑三次
3. 对比 `error_struct.castt.mean_p_traj` 与 `mean_v_traj`
